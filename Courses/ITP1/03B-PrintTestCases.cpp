#include <bits/stdc++.h>

using namespace std;

int main() {
    int index = 1;
    while (true) {
        int x;
        cin >> x;
        if (x == 0) break;
        cout << "Case " << index << ": " << x << endl;
        index++;
    }
}